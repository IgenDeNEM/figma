function toggleControl(client, ...)
    triggerClientEvent(client, "toggleControl", client, ...)
end

function toggleAllControls(client, ...)
    triggerClientEvent(client, "toggleAllControls", client, ...)
end